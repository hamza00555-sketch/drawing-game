# نشر «وش ذا؟»

الواجهة على **Vercel**، والباك-إند على **Firebase**. الاثنان منفصلان ويمكن نشر
كل واحد بمفرده.

---

## متى نحتاج Firebase بالضبط؟

هذا سؤال مهم لأن الإجابة تحدد ترتيب العمل.

| ما تريد فعله | يحتاج مشروع Firebase سحابي؟ |
|---|---|
| بناء الشاشات ومراجعتها | **لا** — معرض المعاينة `?preview=` يعمل ببيانات ثابتة |
| تجربة الرسم والممحاة والتراجع والـReplay | **لا** — يعمل محليًا بالكامل |
| تشغيل الاختبارات (211 اختبارًا) | **لا** |
| إنشاء غرفة والانضمام بكود | **لا** — يكفي المحاكي (القسم ٣) |
| لعب جولة كاملة بكل المودات | **لا** — يكفي المحاكي |
| اختبار الأدوار السرية فعليًا | **لا** — يكفي المحاكي |
| **رابط يفتحه غيرك من جواله** | **نعم** |
| **اختبار انحراف ساعات الأجهزة والانقطاع الحقيقي** | **نعم** |

**الخلاصة:** كل ما بُني حتى الآن قابل للتحقق بدون مشروع Firebase سحابي. بل
**تقدر تلعب اللعبة كاملة الآن** على المحاكي المحلي — راجع القسم ٣، وهو أسرع
طريق وأقلّه احتكاكًا. تحتاج مشروعًا سحابيًا فقط حين تريد **رابطًا يفتحه غيرك من
جواله.**

بدون أي إعداد، التطبيق يبني وينشر بنجاح لكنه يعرض شاشة الإعداد بدل اللعبة —
هذا هو ما تراه إن لم يكن عندك `.env.local`.

---

## Spark أم Blaze؟

**الوضع الحالي المعتمد: Spark (المجانية).** هذا يعني أن جزءًا من اللعبة يعمل
وجزءًا لا يعمل، والحدّ بينهما واضح:

| المكوّن | Spark |
|---|---|
| Anonymous Auth | ✅ |
| Realtime Database (1GB، 10GB تنزيل/شهر، 100 اتصال متزامن) | ✅ |
| Security Rules | ✅ |
| النشر على Vercel | ✅ (لا نستخدم Firebase Hosting أصلًا) |
| **Cloud Functions** | ❌ **تتطلب Blaze** |
| Cloud Storage | ❌ تتطلب Blaze (ولا نستخدمها حاليًا) |

الـ100 اتصال متزامن أكثر من كافية لغرفة من 10 لاعبين.

### ما الذي يعمل فعليًا على Spark

يعمل: إنشاء غرفة، الانضمام بكود، حجز الشخصيات (بالـtransaction)، الحضور
والانقطاع، انتقال الاستضافة، اختيار المود، ومزامنة الساعة.

لا يعمل: **بدء أي جولة، في أي مود.** كل المنطق الموثوق في Cloud Functions —
اختيار الكلمة، تعيين المزوّر، كتابة الأسرار لكل لاعب، الانتقال بين الأطوار،
حساب النقاط. حين يضغط المضيف «ابدأ» ستظهر رسالة صريحة تقول ذلك، لا خطأ مبهم.

### لماذا لا يمكن نقل هذا المنطق إلى العميل

ليس لسبب تقني، بل لأن **من يوزّع الأدوار يعرفها**. لو وزّعها جهاز المضيف لعرف
المضيف من المزوّر وما الكلمة، وانتهى المود حين يلعب المضيف. لهذا
`playerSecrets` مكتوبة `".write": false` بلا استثناء.

### الطريقان حين تقرر

1. **تفعيل Blaze** — لا تغيير في الكود إطلاقًا. الحصة المجانية داخل Blaze
   مليونا استدعاء شهريًا، وجلسة اختبار كاملة تستهلك عشرات. قد تُحتسب بضعة
   سنتات شهريًا لتخزين حاويات الـFunctions. انتبه: Blaze ليس فيها سقف صارم
   يوقف الخدمة، فقط تنبيهات ميزانية — اضبط تنبيهًا عند أول دولار.
2. **نقل المنطق الموثوق إلى Vercel Serverless Functions** — بدون بطاقة.
   Firebase تبقى الباك-إند (Auth و RTDB و Rules بلا تغيير)، وينتقل مكان تنفيذ
   الكود فقط إلى API routes تستخدم `firebase-admin` بمفتاح Service Account
   وتتحقق من اللاعب بـ`verifyIdToken` تمامًا كما تفعل `onCall`.

---

## 1. Firebase

### إنشاء المشروع

1. من [console.firebase.google.com](https://console.firebase.google.com) أنشئ مشروعًا.
2. **Authentication ← Sign-in method ← Anonymous ← Enable.**
   اللعبة لا تطلب حسابًا من اللاعبين، لكن كل قاعدة أمان تعتمد على `auth.uid`،
   فبدون هذه الخطوة لا شيء يعمل.
3. **Realtime Database ← Create database.**
   اختر المنطقة الأقرب للاعبين — `europe-west1` هي الأقل تأخيرًا للخليج عادةً.
   ابدأ بوضع **locked mode**؛ القواعد ستُنشر في الخطوة التالية.
4. **Project settings ← Your apps ← Web app** واحفظ قيم الإعداد.

### نشر القواعد

```bash
npm install -g firebase-tools
firebase login
firebase use --add            # اختر مشروعك
firebase deploy --only database
```

القواعد في `firebase/database.rules.json`. **لا تشغّل اللعبة بدونها** — هي التي
تمنع لاعبًا من قراءة كلمة لا تخصه أو تعديل نقاطه.

> **استخدم `--only database` دائمًا على Spark.** الأمر المجرّد `firebase deploy`
> سيحاول نشر Functions و Storage معًا ويفشل، لأن كليهما يتطلب Blaze.

### نشر Cloud Functions (يتطلب Blaze)

```bash
cd functions && npm install && cd ..
firebase deploy --only functions
```

بدون هذه الخطوة تعمل الغرف ولا تبدأ الجولات — راجع قسم «Spark أم Blaze؟» أعلاه.

---

## 2. Vercel

### الربط

1. اربط المستودع من [vercel.com/new](https://vercel.com/new).
2. الإعدادات تُقرأ من `vercel.json` تلقائيًا — لا تحتاج تغيير شيء.

### متغيرات البيئة

أضف هذه في **Project Settings ← Environment Variables** لبيئات
Production و Preview و Development:

```
VITE_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN
VITE_FIREBASE_DATABASE_URL
VITE_FIREBASE_PROJECT_ID
VITE_FIREBASE_STORAGE_BUCKET
VITE_FIREBASE_MESSAGING_SENDER_ID
VITE_FIREBASE_APP_ID
```

القيم من `Project settings ← Your apps` في Firebase.

**ملاحظة أمنية:** هذه القيم ليست أسرارًا. إعداد Firebase للويب عام بطبيعته
ويظهر في أي bundle. ما يحمي اللعبة هو `database.rules.json` لا إخفاء المفاتيح.
لا تضع أبدًا مفتاح service account هنا — مكانه Cloud Functions فقط.

### النطاقات المصرّح لها

بعد أول نشر، أضف نطاق Vercel في:
**Firebase Console ← Authentication ← Settings ← Authorized domains**

بدون هذه الخطوة سيفشل تسجيل الدخول المجهول على النطاق المنشور، وسيقف التطبيق
عند شاشة التحميل.

---

## 3. التشغيل محليًا — بلا مشروع Firebase وبلا بطاقة

**هذا أسرع طريق لتلعب اللعبة كاملة، بكل المودات.** محاكي Firebase يشغّل
Auth و Realtime Database **و Cloud Functions** على جهازك، ولا يشترط أي خطة —
قيد Blaze على النشر السحابي فقط.

المتطلب الوحيد الإضافي: **Java** (محاكي Realtime Database مكتوب بها).
تحقق بـ`java -version`؛ إن لم تكن موجودة نزّل Temurin JDK.

```bash
npm install                 # يثبّت firebase-tools محليًا — لا حاجة لتثبيت عام
npm run setup:emulator      # يكتب .env.local ويثبّت اعتماديات functions
```

ثم في طرفيتين:

```bash
npm run emulators           # Auth + Database + Functions (يبني functions أولًا)
npm run dev
```

> `npm run setup:emulator` بدل `cp .env.emulator .env.local`، لأن `cp` ليست
> أمرًا في CMD على Windows، وفشلها صامت: التطبيق يقلع ويعرض شاشة الإعداد وكأنه
> تجاهلك. السكربت يعمل على Windows و macOS و Linux بنفس الطريقة، ويرفض أن يدهس
> `.env.local` يشير إلى مشروع سحابي حقيقي إلا بـ`-- --force`.

افتح الرابط في **ثلاث نوافذ متخفية** (كل نافذة لاعب مستقل، لأن الهوية المجهولة
مخزّنة لكل ملف تعريف). أنشئ غرفة من الأولى وانضم بالكود من الباقيتين.

لوحة المحاكي على `http://127.0.0.1:4000` تريك القاعدة مباشرة — وهي أفضل مكان
لتتأكد بنفسك أن الكلمة ليست في `games/{roomId}/current` ولا في حمولة المزوّر.

هذا يغطي كل شيء عدا ما لا يظهر إلا على شبكة حقيقية: انحراف ساعات الأجهزة،
والانقطاع الفعلي، وسلوك اللمس على جوال حقيقي.

### إعادة تشغيل نظيفة

المحاكي يبدأ بقاعدة فارغة في كل مرة. لمسح الحالة أثناء التشغيل:

```bash
curl -X DELETE 'http://127.0.0.1:9000/.json?ns=demo-wesh-tha'
```

---

## 4. قائمة تحقق قبل أول اختبار حقيقي

### على Spark — يوصلك إلى غرفة تعمل

- [ ] Anonymous Auth مفعّل
- [ ] Realtime Database منشأة (europe-west1)
- [ ] `firebase deploy --only database` نُفّذ
- [ ] متغيرات البيئة مضافة في Vercel للبيئات الثلاث
- [ ] نطاق Vercel مضاف في Authorized domains
- [ ] افتح الرابط على جوالين مختلفين وأنشئ غرفة وانضم بالكود
- [ ] أغلق تبويب المضيف وتأكد أن الاستضافة انتقلت لأقدم لاعب متصل

عند هذه النقطة يعمل كل شيء عدا بدء الجولة، والضغط على «ابدأ» يقول لك ذلك صراحة.

### لتشغيل الجولات — يحتاج القرار

- [ ] Blaze مفعّلة **أو** المنطق منقول إلى Vercel Functions
- [ ] `firebase deploy --only functions` نُفّذ (في حالة Blaze)
- [ ] جولة كاملة من «ابدأ» حتى شاشة النقاط
- [ ] **اختبار السرية:** بحساب المزوّر، افتح DevTools واستمع مباشرةً إلى
      `playerSecrets` و`games/{roomId}/current` — يجب ألا تظهر الكلمة في أي
      منهما قبل الـreveal
